import {Schema} from "mongoose";

const {
    Array, Boolean, Buffer, Date, Decimal128, DocumentArray,
    Embedded, Map, Mixed, Number, ObjectId, String
} = Schema.Types;

export {
    Array, Boolean, Buffer, Date, Decimal128, DocumentArray,
    Embedded, Map, Mixed, Number, ObjectId, String
};
export const Virtual = "VIRTUAL";
